# TODO: Document title

TODO: Write the document summary here.

## Section

Normal GitHub-flavored Markdown works here, including tables, lists, links, fenced code, and images.

### Math example

Inline math: $E = mc^2$.

Display math:

$$
L(\theta) = -\sum_i y_i \log p_\theta(y_i)
$$

### Relative image example

Place images in an `_assets/` directory beside the document, then reference them relatively:

```md
![Figure](_assets/figure.png)
```

TODO: Replace the examples above with your content.
